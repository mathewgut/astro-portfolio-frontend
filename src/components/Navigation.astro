---
const buttonClasses = "uppercase font-semibold hover:underline hover:cursor-pointer"
---

<nav class="flex flex-col gap-2 justify-center items-center fixed top-0 pt-2">
    <section class="flex gap-2">
        <button class={buttonClasses}>
            Home
        </button>
        <button class={buttonClasses}>
            Blog
        </button>
        <button class={buttonClasses}>
            Work
        </button>
        <button class={buttonClasses}>
            About
        </button>
        <button class={buttonClasses}>
            Contact
        </button>
    </section>
    <div class="w-35 h-0.5 bg-black" />
</nav>